{
  "name": "reactdux",
  "description": "A collection of javascript functions to help orchestrate react + redux applications.",
  "version": "0.10.2",
  "author": "Steve Binder <email@stevebinder.com>",
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/stevebinder/reactdux"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && NODE_ENV=production babel src/index.js --out-file dist/index.js",
    "prepare": "npm run build",
    "release": "git push origin master && git pull origin master && npm publish"
  },
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-redux": "^5.0.0",
    "redux": "^3.7.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.22.1",
    "babel-preset-babili": "^0.1.4",
    "babel-preset-env": "^1.6.0",
    "babel-preset-react": "^6.22.0",
    "babel-preset-stage-2": "^6.22.0"
  }
}
